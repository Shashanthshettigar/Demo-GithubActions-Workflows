apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
spec:
  replicas: 2

  selector:
    matchLabels:
      app: payment-service

  template:
    metadata:
      labels:
        app: payment-service

    spec:
      containers:
        - name: payment-service
          image: 066288112657.dkr.ecr.us-east-1.amazonaws.com/github-actions-repo:latest
          ports:
            - containerPort: 8080
